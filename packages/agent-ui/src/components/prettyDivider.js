import React from 'react';
import Divider from 'material-ui/Divider';

/**
 * A simple divider with equal spacing to the top and bottom.
 */
const PrettyDivider = () => (
  <Divider style={{ marginTop: '0.5em', marginBottom: '0.5em' }} />
);

export default PrettyDivider;
